<Window x:Class="CatalystGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatalystGUI"
        mc:Ignorable="d"
        Title="Catalyst Project (Confidential)" Height="780" Width="840">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="12pt"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="12pt"/>
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FontSize" Value="12pt"/>
        </Style>
    </Window.Resources>

    
    <Grid Name="UIgrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Arduino control grid-->
        <Grid Name="ArduinoGrid"  Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--connect and COM port-->
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button Click="ConnectUSB_Click" Content="Connect USB"/>
                <ComboBox x:Name="PortSelector" MinWidth="100"/>
            </StackPanel>

            <!--temperature control-->
            <StackPanel Grid.Row="1" Margin="0,5,0,0" Orientation="Horizontal">
                <TextBlock Text="Temp" Width="50"/>
                <TextBlock  Text="{Binding Temperature1}" MinWidth="50"/>
                <Button Click="Temp1Minus_Click" Content="-" Width="20"/>
                <TextBox Text="{Binding Temperature1Set}" MinWidth="40"/>
                <Button Click="Temp1Plus_Click" Content="+" Width="20"/>
                <Button Click="Temp1Stop_Click" Content="Stop"/>

            </StackPanel>

            <!--fan control-->
            <StackPanel Grid.Row="2" Margin="0,5,0,0" Orientation="Horizontal">
                <TextBlock Text="Fan %" Width="100"/>
                <!--<TextBlock  Text="{Binding Fan}" MinWidth="40"/>-->
                <Button Click="FanMinus_Click" Content="-" Width="20"/>
                <TextBox Text="{Binding Fan}" MinWidth="40"/>
                <Button Click="FanPlus_Click" Content="+" Width="20"/>
                <Button Click="FanStop_Click" Content="Stop"/>

            </StackPanel>

            <!--needle control-->
            <StackPanel Grid.Row="3" Margin="0,5,0,0" Orientation="Horizontal">
                <TextBlock Text="Ndl x" Width="100"/>
                <!--<TextBlock  Text="{Binding Fan}" MinWidth="40"/>-->
                <Button Click="NeedlePositionMinus_Click" Content="-" Width="20"/>
                <TextBox Text="{Binding Potentiometer}" MinWidth="40"/>
                <Button Click="NeedlePositionPlus_Click" Content="+" Width="20"/>
                <Button Click="NeedleStop_Click" Content="Stop"/>
            </StackPanel>

            <!--pressures display (auto generated by items control)-->
            <ItemsControl x:Name="PressuresItemsControl" Grid.Row="4">

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DataTemplate.Resources>
                            <!--Makes it inherit style from the outside-->
                            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}"/>
                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"/>
                        </DataTemplate.Resources>
                        
                        <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                            <TextBlock Text="{Binding DisplayName}" Width="60"/>
                            <TextBlock  Text="{Binding Value}" MinWidth="40"/>
                            <Button Click="PressureMinus_Click" Content="-" Width="20"/>
                            <TextBox Text="set" MinWidth="40"/>
                            <Button Click="PressurePlus_Click" Content="+" Width="20"/>
                            <Button Click="MotorStop_Click" Content="Stop"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                              
            </ItemsControl>
            


        </Grid>
        <!--end arduino grid-->

        <!--Grid Splitter that enables resizing of each cell-->
        <GridSplitter HorizontalAlignment="Right" 
                  VerticalAlignment="Stretch" 
                  Grid.Column="1" ResizeBehavior="PreviousAndNext"
                  Width="3" Background="#FFBCBCBC"/>
        <!--x ################################################  x-->

        <!--Main camera grid that holds everything in rows-->
        <Grid Name="CameraGrid" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--Holds the row of live,stop,capture,commandline elements in columns-->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Name="StartCamButton" Content="Start Cam" Click="StartCam_Click"/>
                <Button Grid.Column="1" Name="LiveButton" Content="Live" Click="Live_Click"/>
                <Button Grid.Column="2" Name="StopButton" Content="Stop" Click="Stop_Click"/>
                <Button Grid.Column="3" Name="CaptureButton" Content="Capture" Click="Capture_Click"/>
                <TextBox Grid.Column="4" Name="CommandLine" KeyDown="CommandLine_KeyDown"/>
            </Grid>

            <!--Each control is a vertical StackPanel of label/controls. Controls is [-][textbox][+]-->
            <UniformGrid Rows="1" Grid.Row="1">
                <StackPanel Orientation="Vertical">
                    <!--label-->
                    <TextBlock Text="Exposure (micros)" TextAlignment="Center"/>
                    <!--exposure controls-->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="-" Width="20" Click="ExposureMinus"/>
                        <TextBox Width="70pt" Text="{Binding ExposureTime}"/>
                        <Button Content="+" Width="20" Click="ExposurePlus"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical">
                    <!--label-->
                    <TextBlock Text="Frame Rate (Hz)" TextAlignment="Center"/>
                    <!--framerate controls-->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="---" Click="FrameRateMinus3" Width="35" Margin="0,0,5,0"/>
                        <Button Content="--" Click="FrameRateMinus2" Width="25" Margin="0,0,5,0"/>
                        <Button Content="-" Click="FrameRateMinus" Width="20"/>
                        <TextBox Width="70pt" Text="{Binding FrameRate}"/>
                        <Button Content="+" Click="FrameRatePlus" Width="20" Margin="0,0,5,0"/>
                        <Button Content="++" Click="FrameRatePlus2" Width="25" Margin="0,0,5,0"/>
                        <Button Content="+++" Click="FrameRatePlus3" Width="35"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical">
                    <!--label-->
                    <TextBlock Text="Frame Count" TextAlignment="Center"/>
                    <!--count controls-->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="-" Width="20" Click="FrameCountMinus"/>
                        <TextBox Width="70pt" Text="{Binding FrameCount}"/>
                        <Button Content="+" Width="20" Click="FrameCountPlus"/>
                    </StackPanel>
                </StackPanel>
            </UniformGrid>

            <!--Big UI Image-->
            <Image Name ="imageBox" Source="{Binding UIimage}" Margin="5" Grid.Row="2"/>

            <!--sam: the "selected item" binds the object selected (calls "set") to a property in the context inherited by ListBox-->
            <ListBox ItemsSource="{Binding ImageSourceFrames}" SelectedItem="{Binding UIimage}"  Height="110" Grid.Row="3">
                <!--sam: once inside here, the context gets updated to the objects inside the listbox. If you want to bind to the object itself use
            the {Binding .}, else use the property name straight up. E.g. if listbox has inside a object containing "string txt" you can do
            {Binding txt} and it will show the text-->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding .}" MaxHeight="80"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

            </ListBox>
        </Grid>



    </Grid>
</Window>